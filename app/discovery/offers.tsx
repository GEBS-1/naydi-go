'use client';
import {useEffect,useRef,useState,type ReactNode} from 'react';
import {MapPin,Navigation,Store} from 'lucide-react';
import {onMap,type Offer} from '@/lib/discovery';
import type {Shop} from '@/lib/model';
import {routeTo} from '../store-contacts';
export function Evidence({offer:o}:{offer:Offer}){return <div className="finder-evidence"><p><MapPin size={14}/>{o.address||'Адрес не подтверждён'}{o.distanceKm!==null&&` · ${o.distanceKm.toFixed(1)} км по прямой`}</p><p>{o.kind==='service'?'Услуга · доступность и сроки уточняйте':o.confirmed?'Остаток указан магазином':'Наличие в физическом магазине не подтверждено'}</p><p><a href={o.source} target={o.source.startsWith('http')?'_blank':undefined} rel="noopener noreferrer">{o.source.startsWith('http')?'Первоисточник ↗':'Данные магазина в НайдиGo'}</a><span>{o.checkedAt?' · Проверено '+new Date(o.checkedAt).toLocaleDateString('ru-RU'):' · Дата проверки не указана'}</span></p></div>}
export function OfferRow({offer,card}:{offer:Offer;card:ReactNode}){return <article className="finder-offer">{card}<Evidence offer={offer}/><div className="finder-offer-actions"><a href={'#product/'+offer.id}>{offer.kind==='service'?'Об услуге':'К товару'}</a>{onMap(offer.shop)&&<a href={routeTo(offer.shop)} target="_blank" rel="noopener noreferrer"><Navigation size={16}/>Маршрут</a>}</div></article>}
export default function OffersMap({stores,offers,onSelect,selectedId}:{stores:Shop[];offers:Offer[];selectedId?:string;onSelect?:(id:string)=>void}){const points=stores.filter(onMap);const [selected,setSelected]=useState('');useEffect(()=>{if(selectedId)setSelected(selectedId);},[selectedId]);const [width,setWidth]=useState(600),[offset,setOffset]=useState(0),[failed,setFailed]=useState(false);const ref=useRef<HTMLDivElement>(null);useEffect(()=>{const r=new ResizeObserver(e=>setWidth(e[0].contentRect.width));if(ref.current)r.observe(ref.current);return()=>r.disconnect();},[points.length>0]);const shop=points.find(s=>s.id===selected)||points[0];if(!points.length)return <div className="finder-map-empty"><MapPin size={36}/><h3>Нет подтверждённых точек</h3><p>Предложения без проверенного адреса остаются в списке. Случайные точки на карте не показываем.</p></div>;const project=(s:Shop)=>[(s.lng+180)/360,(1-Math.log(Math.tan(s.lat*Math.PI/180)+1/Math.cos(s.lat*Math.PI/180))/Math.PI)/2];const ps=points.map(project),xs=ps.map(p=>p[0]),ys=ps.map(p=>p[1]),minX=Math.min(...xs),maxX=Math.max(...xs),minY=Math.min(...ys),maxY=Math.max(...ys),height=450;const fit=Math.min(15,Math.floor(Math.log2(Math.min(Math.max(100,width-60)/(256*(maxX-minX||.001)),(height-120)/(256*(maxY-minY||.001))))));const z=Math.max(3,Math.min(18,fit+offset)),scale=256*2**z,left=(minX+maxX)*scale/2-width/2,top=(minY+maxY)*scale/2-height/2,tiles=[];for(let x=Math.floor(left/256);x<=Math.floor((left+width)/256);x++)for(let y=Math.floor(top/256);y<=Math.floor((top+height)/256);y++)tiles.push(<image key={`${z}/${x}/${y}`} href={`https://tile.openstreetmap.org/${z}/${x}/${y}.png`} x={x*256-left} y={y*256-top} width={256} height={256} onError={()=>setFailed(true)}/>);return <div className="finder-map" ref={ref}><svg width="100%" height={height} aria-label="Магазины и услуги на карте">{tiles}{points.map((s,i)=>{const p=project(s),x=p[0]*scale-left,y=p[1]*scale-top;return <g key={s.id} role="button" tabIndex={0} aria-label={'Показать '+s.name} onClick={()=>{setSelected(s.id);onSelect?.(s.id);}} onKeyDown={e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();setSelected(s.id);onSelect?.(s.id);}}}><circle cx={x} cy={y} r={18} fill={shop.id===s.id?'#0043cb':'#0672ff'} stroke="white" strokeWidth={3}/><text x={x} y={y+5} fill="white" textAnchor="middle">{i+1}</text></g>})}</svg><div className="finder-map-buttons"><button onClick={()=>setOffset(Math.min(offset+1,3))} aria-label="Приблизить">+</button><button onClick={()=>setOffset(Math.max(offset-1,-3))} aria-label="Отдалить">−</button></div><div className="finder-map-card"><Store size={22}/><div><a href={shop.id.startsWith('osm:')?shop.source:'#store/'+shop.id}><b>{shop.name}</b></a><p>{shop.street}</p><p>{offers.find(o=>o.shop.id===shop.id)?.product.name||shop.category}</p><a className="finder-primary" href={routeTo(shop)} target="_blank" rel="noopener noreferrer">Маршрут<Navigation size={16}/></a></div></div>{failed&&<p className="finder-map-warning">Подложка недоступна. Адреса и маршруты доступны в карточках.</p>}<a className="finder-map-credit" href="https://www.openstreetmap.org/copyright" target="_blank" rel="noopener noreferrer">© OpenStreetMap contributors</a></div>}
