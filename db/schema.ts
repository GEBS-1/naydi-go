import {sqliteTable,text,integer,index,primaryKey} from 'drizzle-orm/sqlite-core';
export const shops=sqliteTable('shops',{id:text('id').primaryKey(),owner:text('owner'),visibility:text('visibility').notNull().default('public'),data:text('data').notNull()},t=>[index('idx_shops_owner').on(t.owner)]);
export const onboarding=sqliteTable('onboarding',{storeId:text('store_id').primaryKey().references(()=>shops.id),createdBy:text('created_by').notNull(),createdAt:text('created_at').notNull(),consent:text('consent'),confirmedAt:text('confirmed_at')});
export const previewTokens=sqliteTable('preview_tokens',{hash:text('hash').primaryKey(),storeId:text('store_id').notNull().references(()=>shops.id),expiresAt:text('expires_at').notNull(),revoked:integer('revoked').notNull().default(0)},t=>[index('idx_preview_store').on(t.storeId)]);
export const ownerInvites=sqliteTable('owner_invites',{hash:text('hash').primaryKey(),storeId:text('store_id').notNull().references(()=>shops.id),email:text('email').notNull(),expiresAt:text('expires_at').notNull(),usedAt:text('used_at')},t=>[index('idx_invites_store').on(t.storeId)]);
export const products=sqliteTable('products',{id:text('id').primaryKey(),storeId:text('store_id').notNull().references(()=>shops.id),published:integer('published').notNull(),data:text('data').notNull()},t=>[index('idx_products_store').on(t.storeId),index('idx_products_published').on(t.published)]);
export const inquiries=sqliteTable('inquiries',{id:text('id').primaryKey(),storeId:text('store_id').notNull().references(()=>shops.id),customer:text('customer').notNull(),data:text('data').notNull()},t=>[index('idx_inquiries_customer').on(t.customer),index('idx_inquiries_store').on(t.storeId)]);
export const claims=sqliteTable('claims',{id:text('id').primaryKey(),storeId:text('store_id').notNull().references(()=>shops.id),applicant:text('applicant').notNull(),status:text('status').notNull(),data:text('data').notNull()},t=>[index('idx_claims_applicant').on(t.applicant)]);
export const searches=sqliteTable('searches',{id:text('id').primaryKey(),query:text('query').notNull(),resultCount:integer('result_count').notNull(),createdAt:text('created_at').notNull()});
export const favorites=sqliteTable('favorites',{customer:text('customer').notNull(),productId:text('product_id').notNull()},t=>[primaryKey({columns:[t.customer,t.productId]})]);
export const uploads=sqliteTable('uploads',{id:text('id').primaryKey(),owner:text('owner').notNull(),mime:text('mime').notNull()});
export const shoppingPlans=sqliteTable('shopping_plans',{id:text('id').primaryKey(),owner:text('owner').notNull(),data:text('data').notNull()},t=>[index('idx_plans_owner').on(t.owner)]);
export const connectionRequests=sqliteTable('connection_requests',{id:text('id').primaryKey(),data:text('data').notNull(),status:text('status').notNull().default('pending'),createdAt:text('created_at').notNull()});
export const discoverySources=sqliteTable('discovery_sources',{id:text('id').primaryKey(),data:text('data').notNull()});
export const discoveryLimits=sqliteTable('discovery_limits',{id:text('id').primaryKey(),count:integer('count').notNull()});
export const searchCache=sqliteTable('search_cache',{id:text('id').primaryKey(),data:text('data').notNull(),expiresAt:integer('expires_at').notNull()},t=>[index('idx_search_cache_expiry').on(t.expiresAt)]);
export const searchLocks=sqliteTable('search_locks',{id:text('id').primaryKey(),untilMs:integer('until_ms').notNull()});
export const externalPlaces=sqliteTable('external_places',{id:text('id').primaryKey(),city:text('city').notNull(),category:text('category').notNull(),data:text('data').notNull(),checkedAt:integer('checked_at').notNull()},t=>[index('idx_external_city_category').on(t.city,t.category)]);
export const searchMetrics=sqliteTable('search_metrics',{id:text('id').primaryKey(),data:text('data').notNull(),createdAt:integer('created_at').notNull()});
export const apiBudget=sqliteTable('api_budget',{month:text('month').primaryKey(),committed:integer('committed').notNull().default(0)});
export const apiCalls=sqliteTable('api_calls',{id:text('id').primaryKey(),month:text('month').notNull(),provider:text('provider').notNull(),model:text('model').notNull(),reserved:integer('reserved').notNull(),actual:integer('actual'),status:text('status').notNull(),data:text('data').notNull(),createdAt:integer('created_at').notNull()},t=>[index('idx_api_calls_month').on(t.month)]);
